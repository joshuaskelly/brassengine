/**
 * Module for platform specific APIs.
 *
 * The api of this module will differ per platform. Use the name field
 * to determine which platform you are working with.
 *
 * @usage
 * platform = require("platform")
 *
 * if platform.name == "desktop" then
 *    -- Desktop specific logic
 * elseif platform.name == "web" then
 *    -- Web specific logic
 * end
 *
 * @module platform
 */